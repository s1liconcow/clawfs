name: pjdfstest

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  pjdfstest:
    name: Run pjdfstest FUSE Test Suite
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y fuse3 build-essential autoconf automake libtool git perl

          # Enable allow_other for FUSE mounts
          echo "user_allow_other" | sudo tee -a /etc/fuse.conf

      - name: Clone pjdfstest
        run: |
          git clone https://github.com/pjd/pjdfstest.git /tmp/pjdfstest
          cd /tmp/pjdfstest
          autoreconf -fvi
          ./configure
          make

      - uses: dtolnay/rust-toolchain@stable

      - name: Build ClawFS
        run: cargo build --release --bin clawfsd

      - name: Start ClawFS
        run: |
          mkdir -p /tmp/clawfs-mnt /tmp/clawfs-store /tmp/clawfs-cache

          ./target/release/clawfsd \
            --mount-path /tmp/clawfs-mnt \
            --store-path /tmp/clawfs-store \
            --local-cache-path /tmp/clawfs-cache \
            --state-path /tmp/clawfs-state.bin \
            --object-provider local \
            --log-file /tmp/clawfs-mount.log \
            --allow-other \
            --disable-journal \
            --disable-cleanup \
            --foreground &
          echo $! > /tmp/clawfs.pid

          # Wait for mount to come up
          echo "Waiting for ClawFS mount..."
          for i in $(seq 1 30); do
            if mountpoint -q /tmp/clawfs-mnt 2>/dev/null; then
              echo "ClawFS is mounted"
              break
            fi
            sleep 1
          done

          if ! mountpoint -q /tmp/clawfs-mnt 2>/dev/null; then
            echo "ClawFS failed to mount"
            cat /tmp/clawfs.log 2>/dev/null || true
            exit 1
          fi

      - name: Run pjdfstest
        working-directory: /tmp/clawfs-mnt
        run: |
          mkdir -p pjdfstest_run
          cd pjdfstest_run

          # Get list of all test files
          find /tmp/pjdfstest/tests -name "*.t" -type f > /tmp/all_tests.txt

          # Filter out excluded tests
          if [ -f ${{ github.workspace }}/.github/.pjdfstest-exclude ]; then
            grep -v -f ${{ github.workspace }}/.github/.pjdfstest-exclude /tmp/all_tests.txt > /tmp/tests_to_run.txt || true
          else
            cp /tmp/all_tests.txt /tmp/tests_to_run.txt
          fi

          # Run the filtered test suite
          sudo prove -rv $(cat /tmp/tests_to_run.txt)

      - name: Upload ClawFS logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: clawfs-mount-log
          path: /tmp/clawfs-mount.log
          if-no-files-found: ignore

      - name: Cleanup
        if: always()
        run: |
          cd /tmp
          if [ -f /tmp/clawfs.pid ]; then
            kill $(cat /tmp/clawfs.pid) || true
            sleep 2
          fi
          sudo umount -l /tmp/clawfs-mnt || fusermount -u /tmp/clawfs-mnt || true
